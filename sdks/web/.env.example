# SDK Configuration (for development only)
# These are typically set at runtime, not build time

# API endpoint (optional, defaults to current origin)
# VITE_API_URL=http://localhost:3000

# WebSocket endpoint (optional, defaults to current origin)
# VITE_WS_URL=ws://localhost:3000
