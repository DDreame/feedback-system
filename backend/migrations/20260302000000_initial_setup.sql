-- Initial database setup
-- Enable UUID generation support
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Note: pgvector extension (for AI embeddings) will be enabled in Phase 3
-- CREATE EXTENSION IF NOT EXISTS vector;
